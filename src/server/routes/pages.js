const db = require('../db/dbhelper');
const apiResult = require('../utils/apiResult');
const _token = require('../token/token');

module.exports = {
    accountmanage:(app) => {
        app.get('/accountmanage.html',(request,response) => {
            
        })
    }
}

